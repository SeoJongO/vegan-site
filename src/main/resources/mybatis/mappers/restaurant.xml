<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="restaurant">



<!-- 한명의 정보가져오기 -->
<select id="getOne" parameterType="int" resultType="com.javaex.vo.OwnerVo" >

<![CDATA[
select
	    s.s_no,
	    s.u_no, 
	    s.s_img, 
	    s.s_name, 
	    s.s_phone, 
	    s.s_address,
	    s.s_detail_address, 
	    s.s_openH, 
	    s.s_openM, 
	    s.s_closeH, 
	    s.s_closeM, 
	    s.s_parking, 
	    s.s_intro, 
	    s.s_youtube, 
	    s.s_instagram, 
	    s.s_twitter, 
	    s.s_Hmon, 
	    s.s_Htue,
	    s.s_Hwen,
	    s.s_Hthu,
	    s.s_Hfri,
	    s.s_Hsat,
	    s.s_Hsun,
	    s.s_latitude,
	    s.s_longitude,
        m.m_name,
        m.m_imge,
        m.m_price,
        m.m_type,
        m.m_intro
 	   from store s, menu m
       where s.s_no = m.s_no
       and s.s_no = #{s_no}
       and rownum=1

]]>

</select>

<!-- 메뉴가져오기 -->
<select id="menuList" parameterType="int" resultType="com.javaex.vo.MenuVo">

<![CDATA[
  select 	m_no,
              s_no,
              m_name,
              m_imge,
              m_type,
              m_intro,
              m_price
       from menu
       where s_no= #{s_no}
]]>


</select>

<!-- 리뷰쓰기 -->
<insert id="insertReview" parameterType="com.javaex.vo.ReviewVo" >

<![CDATA[
	INSERT into review
	VALUES (r_no.nextval,#{s_no},#{u_no},#{r_contents}, sysdate ,'5점',#{saveName})
]]>
</insert>



<!-- 리뷰리스트가져오기 -->

<select id="reviewList" parameterType="int" resultType="com.javaex.vo.ReviewVo">


<![CDATA[
select r.r_no,
	   r.s_no,
	   r.u_no,
	   r.r_contents,
	   r.r_date,
	   r.star,
	   r.saveName,
	   u.u_nickname,
	   u.u_type
	   from review r, users u
	   where r.u_no = u.u_no 
	   and r.s_no=#{s_no}

]]>




</select>


<select id="reviewModi" parameterType="int" resultType="com.javaex.vo.ReviewVo">

<![CDATA[

	  select r_no,
              s_no,
              r_no,
              r_contents,
              r_date,
              star,
              savename
                
       from review
       where r_no=#{reviewNo}




]]>






</select>










</mapper>