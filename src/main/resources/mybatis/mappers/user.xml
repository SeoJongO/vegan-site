<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	
	<select id="getUser" parameterType = "com.javaex.vo.UserVo" resultType = "com.javaex.vo.UserVo">
		<![CDATA[
			select	user_no,
					nickname,
					password,
					vegan_type
			from users
			where email = #{email}
			and password = #{password}
       	]]>
	</select>
	
	<insert id="joinUser" parameterType = "com.javaex.vo.UserVo">
		<![CDATA[
			insert into users 
			values(seq_user_no.nextval, #{email}, #{password}, #{nickname}, #{gender}, #{phonenumber}, #{vegan_type})
		]]>
	</insert>
	
	<select id="userInfo" parameterType = "int" resultType = "com.javaex.vo.UserVo">
		<![CDATA[
			select	email,
					password,
					nickname,
					gender,
					phonenumber,
					vegan_type
			from users
			where user_no = #{user_no}
       	]]>
	</select>
	
	<select id="userUpdate" parameterType = "com.javaex.vo.UserVo">
		<![CDATA[
			update users
			set password = #{password},
				nickname = #{nickname},
				gender = #{gender},
				phonenumber = #{phonenumber},
				vegan_type = #{vegan_type}
			where email = #{email}
       	]]>
	</select>
	
	<select id="getEmail" parameterType = "String" resultType="String">
		<![CDATA[
			select email
			from users
			where phonenumber = #{phonenumber}
       	]]>
	</select>

</mapper>
